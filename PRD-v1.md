# PC 자산관리 시스템 — v1 PRD (MVP)

| 항목 | 내용 |
|---|---|
| 문서명 | PC 자산관리 시스템 v1 PRD |
| 상위 문서 | `PRD-Master.md` |
| 버전 | v0.1 (Draft) |
| 최종 수정일 | 2026-08-26 |
| 상태 | 논의 진행 중 |

---

## 1. v1 목표

> **한 문장 목표: IT 관리자가 엑셀 대장을 더 이상 열지 않게 만든다.**

v1의 성공은 기능 개수가 아니라 **엑셀 대장의 완전한 대체**로 판단한다. 기존 대장이 전량 이관되고, 관리자의 일상 업무(등록·배정·변경·조회·보고)가 시스템 안에서 끝나면 v1은 성공이다.

### 1.1 v1 완료 조건 (Definition of Done)

- [ ] 기존 엑셀 대장 전량이 오류 없이 시스템에 이관됨
- [ ] 관리자가 신규 자산 등록부터 배정까지 시스템만으로 수행
- [ ] 부서 이동·자리 이동 변경이 이력과 함께 기록됨
- [ ] 대시보드에서 사업장별·부서별 현황을 즉시 확인 가능
- [ ] 임의 조건으로 검색한 결과를 엑셀로 내보내 보고 가능
- [ ] 2주간 병행 운영 후 엑셀 대장 갱신을 중단

### 1.2 v1 제외 항목

다음은 명시적으로 v1에서 제외한다. (v2/v3 참조)

| 제외 | 사유 |
|---|---|
| 자산 실사 기능 | 실사 방식(현장 태블릿 vs 사무실 입력)이 미확정. 이관·안정화가 우선 |
| 수리·부품교체 이력 | 이력 프레임(FR-10)만 갖추면 v2에서 유형 추가로 확장 가능 |
| 첨부파일 | 저장소·용량 정책 미확정 |
| 다중 권한 / AD 연동 | 사용자가 IT 관리자 소수. 단일 역할로 충분 |
| 이메일 알림 | 폐쇄망 메일 서버 연동 확인 필요. v1은 화면 표출로 대체 |
| 폐기 결재 워크플로 | 상태 전환 + 사유 기록으로 v1 대응 |

---

## 2. 사용자 및 권한

**v1은 단일 역할 — IT 관리자 전용**이다.

| 역할 | 권한 |
|---|---|
| IT 관리자 (`ADMIN`) | 모든 기능 사용 가능 |

- 계정은 시스템 관리자가 화면에서 직접 생성한다 (초기 관리자 계정은 설치 시 시딩).
- 예상 계정 수: 3~5개.
- 향후 역할 확장을 위해 **DB에는 `role` 컬럼을 처음부터 둔다**. v1에서는 값이 항상 `ADMIN`이다. → 나중에 테이블 마이그레이션 없이 v3 확장 가능.

---

## 3. 기능 요구사항

### FR-01. 관리자 로그인

**사용자 스토리**
> IT 관리자로서, 사내 계정 정보로 로그인하여 나만 자산 정보에 접근할 수 있게 하고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 01-1 | ID(영문/숫자) + 비밀번호로 로그인한다 | Must |
| 01-2 | 비밀번호는 해시로 저장한다. 평문 저장·로그 출력 금지 | Must |
| 01-3 | 로그인 5회 연속 실패 시 해당 계정을 10분간 잠근다 | Must |
| 01-4 | 세션 유효시간은 8시간. 만료 시 로그인 화면으로 이동한다 | Must |
| 01-5 | 로그인하지 않은 상태로 모든 화면 URL 직접 접근 시 로그인 화면으로 리다이렉트한다 | Must |
| 01-6 | 본인 비밀번호 변경 화면을 제공한다 (현재 비밀번호 확인 후 변경) | Must |
| 01-7 | 관리자 계정 목록·생성·비활성화 화면을 제공한다 | Should |
| 01-8 | 비밀번호 정책: 8자 이상, 영문+숫자 조합 필수 | Should |

**비밀번호 초기화**: v1에서는 별도 관리자가 대상 계정의 비밀번호를 임시값으로 재설정하는 방식으로 처리한다. (메일 발송 없음)

---

### FR-02. 자산 등록 (수동)

**사용자 스토리**
> IT 관리자로서, 새로 입고된 노트북을 시스템에 등록하여 관리 대상에 포함시키고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 02-1 | Master §7의 필드 중 v1 대상 항목을 입력할 수 있다 | Must |
| 02-2 | 필수 항목 미입력 시 저장을 차단하고 해당 필드에 오류를 표시한다 | Must |
| 02-3 | 자산번호가 이미 존재하면 저장을 차단하고 "이미 등록된 자산번호입니다 (자산: OOO)"를 안내한다 | Must |
| 02-4 | 시리얼번호가 이미 존재하면 저장을 차단한다 (값이 있는 경우) | Must |
| 02-5 | Hostname·IP 주소가 중복되면 **경고**를 표시하되 확인 후 저장은 허용한다 | Should |
| 02-6 | 신규 등록 시 기본 상태는 `대기`다 | Must |
| 02-7 | 등록 화면에서 곧바로 사용자 배정을 함께 입력할 수 있다 (선택). 배정 정보 입력 시 상태는 `사용중`으로 설정된다 | Should |
| 02-8 | 저장 성공 시 `CREATE` 이력이 자동 기록된다 (등록방식=수동, 등록자, 등록일시) | Must |
| 02-9 | "저장 후 계속 등록" 버튼을 제공한다. 제조사·모델명·사업장 등 직전 입력값을 유지해 연속 등록 부담을 줄인다 | Should |

**입력 검증 규칙**

| 필드 | 검증 |
|---|---|
| 자산번호 | 필수, 유니크, 최대 30자, 영문/숫자/하이픈 |
| 구매일 | 필수, 미래 날짜 불가 |
| IP 주소 | IPv4 형식 (`0.0.0.0`~`255.255.255.255`) |
| MAC 주소 | `XX:XX:XX:XX:XX:XX` 또는 `XX-XX-XX-XX-XX-XX` 형식, 저장 시 대문자 콜론 형식으로 정규화 |
| RAM | 1~1024 사이 정수 (GB) |
| 디스크 용량 | 1~100000 사이 정수 (GB) |
| 지급일 | 구매일보다 이전일 수 없음 |
| 반납예정일 | 지급일보다 이전일 수 없음 |

---

### FR-03. 자산 조회 / 상세

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 03-1 | 자산 목록을 표 형태로 조회한다. 기본 정렬은 최근 등록순 | Must |
| 03-2 | 기본 표시 컬럼: 자산번호 / 자산구분 / 제조사·모델 / 상태 / 사용자명 / 소속부서 / 사업장 / 위치 / 지급일 | Must |
| 03-3 | 기본 필터로 `폐기` 상태 자산은 제외한다 (체크박스로 포함 가능) | Must |
| 03-4 | 페이징: 페이지당 20 / 50 / 100건 선택 | Must |
| 03-5 | 컬럼 헤더 클릭으로 정렬 (자산번호, 구매일, 지급일, 상태) | Should |
| 03-6 | 행 클릭 시 상세 화면으로 이동한다 | Must |
| 03-7 | 상세 화면은 탭으로 구성한다: **기본정보 / 사용자·배정 / 하드웨어 / 이력** | Must |
| 03-8 | 표시 컬럼을 관리자가 선택·저장할 수 있다 | Could |

**상세 화면 구성**

```
┌──────────────────────────────────────────────────────┐
│ PC-2026-0042   [사용중]            [수정] [배정변경] │
│ 삼성 DM500SFA-A58A · 데스크톱                        │
├──────────────────────────────────────────────────────┤
│ [기본정보] [사용자·배정] [하드웨어] [이력]           │
├──────────────────────────────────────────────────────┤
│  현재 사용자   홍길동 (사번 20210315)                │
│  소속부서      생산본부 / 생산기술팀                 │
│  사업장·위치   시화 / B동 2층                        │
│  지급일        2026-03-02                            │
│  반납예정일    —                                     │
│  관리 담당자   김IT                                  │
└──────────────────────────────────────────────────────┘
```

---

### FR-04. 자산 수정

**사용자 스토리**
> IT 관리자로서, RAM을 증설했거나 IP가 바뀐 자산 정보를 수정하고, 무엇이 왜 바뀌었는지 남기고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 04-1 | 자산번호를 제외한 모든 필드를 수정할 수 있다 | Must |
| 04-2 | **자산번호는 수정 불가**다. 오등록 시 삭제 후 재등록한다 | Must |
| 04-3 | 저장 시 **변경 사유 입력이 필수**다 (최대 200자) | Must |
| 04-4 | 실제로 값이 바뀐 필드만 이력에 기록한다. 변경 없는 저장은 이력을 남기지 않는다 | Must |
| 04-5 | 저장 전 "변경 내용 확인" 요약을 표시한다 (필드명 / 이전 값 → 새 값) | Should |
| 04-6 | 사용자·부서·사업장 변경은 `MOVE` 이력, 하드웨어·기본정보 변경은 `UPDATE` 이력으로 분류한다 | Must |

**자산 삭제 (제한적)**

| No | 내용 |
|---|---|
| 04-7 | 삭제는 **등록 후 24시간 이내 + 이력이 `CREATE` 하나뿐인 자산**만 가능하다 |
| 04-8 | 그 외 자산은 삭제 대신 `폐기` 상태 전환을 안내한다 |
| 04-9 | 삭제 시 사유 입력 필수, 삭제 기록은 별도 로그에 남긴다 |

---

### FR-05. 검색 / 필터

**사용자 스토리**
> IT 관리자로서, "시화 사업장 생산기술팀의 노트북 중 사용중인 것"을 즉시 찾고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 05-1 | **통합 검색어** 1개 입력창을 제공한다. 자산번호 / 시리얼번호 / 사용자명 / 사번 / Hostname / IP / 모델명을 부분 일치로 동시 검색한다 | Must |
| 05-2 | 상세 필터: 자산구분, 자산상태(복수 선택), 제조사, 사업장, 부서, 운영체제, 관리 담당자 | Must |
| 05-3 | 기간 필터: 구매일, 지급일, 폐기일 각각 시작~종료 지정 | Must |
| 05-4 | 필터 조합은 AND로 동작하며, 동일 필터 내 복수 선택은 OR로 동작한다 | Must |
| 05-5 | 적용된 필터를 화면 상단에 칩(chip)으로 표시하고 개별 해제할 수 있다 | Should |
| 05-6 | "전체 초기화" 버튼을 제공한다 | Must |
| 05-7 | 검색 조건이 URL에 반영되어 북마크·공유가 가능하다 | Should |
| 05-8 | 자주 쓰는 검색 조건을 이름을 붙여 저장할 수 있다 | Could |

**빠른 필터 (원클릭)**

목록 상단에 상시 노출한다.

- `전체 보유` / `사용중` / `대기` / `수리` / `폐기예정` / `반납예정일 초과` / `사용자 미지정`

---

### FR-06. 엑셀 가져오기

**사용자 스토리**
> IT 관리자로서, 기존 엑셀 대장 1,200행을 한 번에 등록하고, 잘못된 행이 있으면 어디가 왜 틀렸는지 정확히 알고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 06-1 | 표준 템플릿(.xlsx)을 다운로드할 수 있다. 헤더·예시 행·항목 설명 시트를 포함한다 | Must |
| 06-2 | 업로드 파일 형식은 `.xlsx`, `.xls`, `.csv`를 지원한다 | Must |
| 06-3 | 최대 5,000행 / 10MB까지 처리한다 | Must |
| 06-4 | 업로드 즉시 **미리보기(첫 20행)**와 컬럼 매핑 화면을 표시한다 | Must |
| 06-5 | 헤더명이 템플릿과 다르면 관리자가 **수동 매핑**할 수 있다 (엑셀 컬럼 → 시스템 필드) | Must |
| 06-6 | 전체 행을 검증하고 **오류 리포트**를 표시한다 | Must |
| 06-7 | 오류 리포트는 `행번호 / 컬럼 / 입력값 / 오류사유` 형식으로 보여주며, 엑셀로 내려받을 수 있다 | Must |
| 06-8 | **All-or-Nothing 모드**(오류 1건이라도 있으면 전체 취소)와 **부분 반영 모드**(정상 행만 등록)를 선택할 수 있다. 기본값은 All-or-Nothing | Must |
| 06-9 | 기존 자산번호와 중복되는 행은 `신규 등록 건너뛰기` 또는 `기존 정보 갱신` 중 선택할 수 있다 | Should |
| 06-10 | 가져오기 결과 요약을 표시한다: 총 행 / 성공 / 실패 / 건너뜀 | Must |
| 06-11 | 가져오기로 등록된 자산에는 `CREATE` 이력(등록방식=엑셀, 배치ID)이 기록된다 | Must |
| 06-12 | 가져오기 배치 단위 **되돌리기**를 지원한다 (해당 배치로 등록된 자산 중 이후 변경 이력이 없는 건에 한함) | Should |

**검증 항목**

| 구분 | 검증 내용 | 처리 |
|---|---|---|
| 필수값 | 필수 항목 누락 | 오류 |
| 형식 | 날짜·숫자·IP·MAC 형식 불일치 | 오류 |
| 코드값 | 자산구분·상태·사업장이 정의된 코드에 없음 | 오류 (유사값 제안: "데스크탑" → "데스크톱") |
| 중복 | 파일 내 자산번호 중복 | 오류 |
| 중복 | 기존 DB 자산번호와 중복 | 정책에 따라 건너뜀/갱신 |
| 참조 | 사번이 임직원 마스터에 없음 | 경고 (등록은 허용, 확인 목록에 표시) |
| 논리 | 상태=사용중인데 사용자 없음 | 오류 |
| 논리 | 상태=폐기인데 폐기일 없음 | 오류 |
| 정규화 | RAM `"16GB"` → `16`, 날짜 `"2024.03.05"` → `2024-03-05` | 자동 변환 후 미리보기에 표시 |

> **설계 메모**: 오류 리포트의 품질이 이 기능의 성패를 좌우한다. "가져오기 실패"라는 메시지 하나로 끝나면 관리자는 결국 엑셀로 돌아간다. 어느 행, 어느 컬럼, 어떤 값이, 왜 틀렸고, 무엇으로 고치면 되는지까지 제시한다.

---

### FR-07. 엑셀 내보내기

| No | 내용 | 우선순위 |
|---|---|---|
| 07-1 | 현재 검색·필터 조건이 적용된 결과 전체를 `.xlsx`로 내보낸다 (페이징 무관) | Must |
| 07-2 | 내보내기 항목을 선택할 수 있다: `기본 항목` / `전체 항목` / `사용자 지정` | Must |
| 07-3 | 파일명 규칙: `자산현황_YYYYMMDD_HHmm.xlsx` | Must |
| 07-4 | 첫 행은 헤더로 고정하고 틀 고정·자동 필터를 적용한다 | Should |
| 07-5 | 최대 10,000행까지 내보낸다. 초과 시 조건 축소를 안내한다 | Must |
| 07-6 | 자산별 이력을 포함한 상세 내보내기를 별도 시트로 제공한다 | Could |
| 07-7 | 내보내기 실행 시 실행자·일시·조건을 로그에 남긴다 (개인정보 포함 파일 추적) | Should |

---

### FR-08. 자산 배정

**사용자 스토리**
> IT 관리자로서, 신규 입사자에게 노트북을 지급하면서 누구에게 언제 줬는지 기록하고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 08-1 | `대기` 상태 자산만 배정할 수 있다 | Must |
| 08-2 | 배정 시 입력 항목: 사번(필수), 사용자명(자동), 소속부서(자동, 수정 가능), 직급(자동), 사업장(필수), 위치, 지급일(필수, 기본=오늘), 반납예정일(선택), 배정 사유 | Must |
| 08-3 | 사번 입력 시 임직원 마스터에서 자동완성으로 검색한다 (사번 또는 이름으로) | Must |
| 08-4 | 임직원 마스터에 없는 사번은 경고 후 직접 입력을 허용한다 | Should |
| 08-5 | 배정 완료 시 자산 상태가 `사용중`으로 자동 전환된다 | Must |
| 08-6 | `ASSIGN` 이력이 기록된다 (이전 사용자 → 신규 사용자) | Must |
| 08-7 | 한 명에게 **여러 자산 배정이 가능**하다 (데스크톱 + 노트북 동시 사용) | Must |
| 08-8 | 한 자산은 동시에 **한 명에게만** 배정된다 | Must |
| 08-9 | 임직원 상세에서 해당 직원에게 배정된 자산 목록을 확인할 수 있다 | Should |
| 08-10 | 여러 자산을 선택해 동일 사용자에게 일괄 배정할 수 있다 | Could |

**배정 변경 (사용자 교체)**

- `사용중` 자산의 사용자를 직접 교체하는 경우, 시스템은 **회수 → 배정** 두 이력을 자동으로 생성한다.
- 이때 이전 사용자의 회수일은 신규 사용자의 지급일과 동일하게 설정한다.

---

### FR-09. 자산 회수

| No | 내용 | 우선순위 |
|---|---|---|
| 09-1 | `사용중` 또는 `수리` 상태 자산을 회수할 수 있다 | Must |
| 09-2 | 회수 입력 항목: 회수일(필수, 기본=오늘), 회수 사유(필수: 퇴사/부서이동/장비교체/수리/반납/기타), 회수 후 상태(대기/수리/폐기예정), 비고 | Must |
| 09-3 | 회수 완료 시 현재 배정이 종료되고 사용자 정보가 비워진다 | Must |
| 09-4 | 회수 후에도 이력에서 이전 사용자를 확인할 수 있다 | Must |
| 09-5 | `RETURN` 이력이 기록된다 | Must |
| 09-6 | 여러 자산을 선택해 일괄 회수할 수 있다 (퇴사자 일괄 처리) | Should |
| 09-7 | 회수일은 지급일보다 이전일 수 없다 | Must |

---

### FR-10. 변경 이력

**사용자 스토리**
> IT 관리자로서, 이 PC가 지금까지 누구를 거쳐왔고 무엇이 바뀌었는지 한 화면에서 보고 싶다.

**요구사항**

| No | 내용 | 우선순위 |
|---|---|---|
| 10-1 | 자산 상세의 [이력] 탭에서 해당 자산의 전체 이력을 최신순 타임라인으로 표시한다 | Must |
| 10-2 | 각 이력 항목은 다음을 포함한다: 발생일시, 이력유형, 변경자, 변경 사유, 변경 필드별 `이전 값 → 새 값` | Must |
| 10-3 | 이력유형별 필터를 제공한다 (등록/배정/회수/이동/정보변경/상태변경/폐기) | Should |
| 10-4 | **이력은 수정·삭제할 수 없다** (append-only) | Must |
| 10-5 | 전체 자산에 대한 통합 이력 조회 화면을 제공한다 (기간·유형·변경자·자산번호로 필터) | Should |
| 10-6 | 통합 이력을 엑셀로 내보낼 수 있다 | Should |
| 10-7 | 사용자 변경 이력만 별도로 요약한 "사용 이력" 뷰를 제공한다 (사용자 / 사용 시작 / 사용 종료 / 사용 기간) | Should |

**이력 표시 예시**

```
2026-08-20 14:32   [정보변경]   김IT
   사유: RAM 증설 (8GB → 16GB)
   RAM          8  →  16
   디스크 용량  256 →  512

2026-03-02 09:15   [배정]   김IT
   사유: 신규 입사자 배정
   사용자   (없음)  →  홍길동 (20210315)
   부서     (없음)  →  생산본부 / 생산기술팀
   상태     대기    →  사용중

2026-02-28 11:04   [등록]   김IT
   등록방식: 엑셀 일괄 (배치 IMP-20260228-001)
```

---

### FR-11. 대시보드

**사용자 스토리**
> IT 관리자로서, 로그인 직후 전체 자산 현황과 지금 처리해야 할 일을 한눈에 보고 싶다.

**요구사항**

| No | 영역 | 내용 | 우선순위 |
|---|---|---|---|
| 11-1 | 요약 카드 | 전체 보유 / 사용중 / 대기 / 수리 / 폐기예정 / 폐기(누적) 건수. 클릭 시 해당 조건으로 필터된 목록으로 이동 | Must |
| 11-2 | 사업장별 현황 | 사업장 × 상태 교차 집계 표 또는 막대 차트 | Must |
| 11-3 | 부서별 현황 | 부서별 보유 대수 상위 10개 + 전체 보기 | Must |
| 11-4 | 자산구분별 비율 | 데스크톱 / 노트북 / 워크스테이션 비율 | Should |
| 11-5 | 조치 필요 목록 | ① 반납예정일 초과 ② 사용자 미지정 `사용중` 자산 ③ 30일 이상 `수리` 상태 ④ `폐기예정` 미처리 | Must |
| 11-6 | 노후 자산 | 구매 후 내용연수 초과 자산 건수 및 목록 | Should |
| 11-7 | 최근 변경 | 최근 7일 이력 10건 | Should |
| 11-8 | 기간 필터 | 대시보드 상단에서 기준 기간을 선택 (기본: 전체) | Could |

**대시보드 레이아웃 (안)**

```
┌───────────────────────────────────────────────────────────────┐
│  전체 보유    사용중     대기      수리    폐기예정   폐기(누적) │
│    1,284       1,092      142        31        19       213    │
├──────────────────────────────┬────────────────────────────────┤
│  사업장별 현황               │  조치 필요                     │
│  서울   ████████████ 412     │  ⚠ 반납예정일 초과      7건    │
│  시화   ████████ 298         │  ⚠ 사용자 미지정        3건    │
│  판교   ██████ 214           │  ⚠ 30일 이상 수리중     2건    │
│  인천   ████ 156             │  ⚠ 폐기예정 미처리     19건    │
│  발안   ███ 121              │                                │
│  음성   ██  83               │                                │
├──────────────────────────────┼────────────────────────────────┤
│  자산구분별 비율             │  최근 변경 이력                │
│  ● 데스크톱 62%              │  08-25 배정 PC-2026-0044 …    │
│  ● 노트북   35%              │  08-24 회수 PC-2023-0871 …    │
│  ● 워크스테이션 3%           │  08-24 정보변경 PC-2024-… …   │
└──────────────────────────────┴────────────────────────────────┘
```

> **설계 메모**: 대시보드의 핵심은 통계가 아니라 **"조치 필요" 목록**이다. 숫자만 보여주는 대시보드는 며칠 지나면 아무도 보지 않는다. 오늘 뭘 해야 하는지 알려줄 때 매일 열어보는 화면이 된다.

---

### FR-12. 상태 관리

| No | 내용 | 우선순위 |
|---|---|---|
| 12-1 | Master §8.3의 상태 전환 규칙을 시스템이 강제한다. 허용되지 않는 전환은 UI에서 비활성화한다 | Must |
| 12-2 | 상태 변경 시 사유 입력이 필수다 | Must |
| 12-3 | `폐기` 전환 시 폐기일·폐기방법 입력이 필수다 | Must |
| 12-4 | `폐기` 자산은 모든 기본 집계·목록에서 제외된다 | Must |
| 12-5 | `폐기` 자산은 수정할 수 없다 (조회 전용) | Must |
| 12-6 | `사용중` 자산을 바로 `폐기예정`으로 전환하려 하면 "먼저 회수 처리가 필요합니다"를 안내한다 | Must |
| 12-7 | 여러 자산을 선택해 일괄 상태 변경할 수 있다 (동일 사유 적용) | Should |
| 12-8 | `STATUS` 이력이 기록된다 | Must |

---

### FR-13. 공통코드 관리

| No | 내용 | 우선순위 |
|---|---|---|
| 13-1 | 코드 그룹별로 코드값을 등록·수정·정렬·비활성화할 수 있다 | Must |
| 13-2 | 대상 코드 그룹: 자산구분, 제조사, 자산상태, 사업장, 부서, 직급, 운영체제, 디스크유형, 폐기방법, 회수사유 | Must |
| 13-3 | 사용 중인 코드는 삭제할 수 없고 비활성화만 가능하다. 비활성 코드는 신규 입력 선택지에서 제외되나 기존 데이터에는 유지된다 | Must |
| 13-4 | 부서는 `사업부 > 팀` 2단계 계층을 지원한다 | Must |
| 13-5 | 코드는 화면 표시 순서(sort order)를 지정할 수 있다 | Should |

**초기 코드값 (시딩)**

| 그룹 | 값 |
|---|---|
| 자산구분 | 데스크톱, 노트북, 워크스테이션 |
| 자산상태 | 대기, 사용중, 수리, 폐기예정, 폐기 |
| 사업장 | 서울, 인천, 시화, 판교, 발안, 음성 |
| 제조사 | 삼성, LG, Dell, HP, Lenovo, ASUS, Apple, 조립, 기타 |
| 운영체제 | Windows 11, Windows 10, Windows Server, macOS, Linux, 기타 |
| 디스크유형 | HDD, SSD, NVMe |
| 폐기방법 | HDD 물리파기, 완전삭제(디가우징), 업체 반납, 매각, 기타 |
| 회수사유 | 퇴사, 부서이동, 장비교체, 수리, 반납, 기타 |

---

### FR-14. 임직원 마스터

**사용자 스토리**
> IT 관리자로서, 사번만 입력하면 이름·부서가 자동으로 채워져서 오타 없이 배정하고 싶다.

| No | 내용 | 우선순위 |
|---|---|---|
| 14-1 | 임직원 정보를 관리한다: 사번(유니크, 필수), 성명(필수), 소속부서, 직급, 사업장, 재직상태(재직/휴직/퇴사) | Must |
| 14-2 | 엑셀로 임직원 명단을 일괄 가져올 수 있다 (FR-06과 동일한 검증·오류 리포트 방식) | Must |
| 14-3 | 임직원 상세에서 해당 직원에게 배정된 자산 목록을 확인할 수 있다 | Must |
| 14-4 | 재직상태를 `퇴사`로 변경할 때 배정된 자산이 있으면 경고하고 미회수 자산 목록을 표시한다 | Must |
| 14-5 | 임직원의 부서를 변경해도 **기존 자산의 배정 정보는 자동 변경되지 않는다**. 대신 "부서 정보가 다른 자산 N건" 목록을 제공해 일괄 갱신할 수 있게 한다 | Must |
| 14-6 | 임직원은 삭제하지 않고 재직상태로 관리한다 | Must |

> **설계 메모 (14-5)**: 임직원 부서가 바뀔 때 자산 배정 부서를 자동으로 따라 바꾸면 "그 시점에 어느 부서 소속으로 쓰고 있었는지"라는 이력이 소급 변조된다. 자동 반영 대신 **차이를 보여주고 관리자가 이력과 함께 갱신**하도록 한다.

---

### FR-15. 반납 예정 알림 (화면)

| No | 내용 | 우선순위 |
|---|---|---|
| 15-1 | 반납예정일이 오늘 이전인 `사용중` 자산을 대시보드 "조치 필요"에 표시한다 | Must |
| 15-2 | 반납예정일 7일 이내 도래 자산을 별도로 표시한다 | Should |
| 15-3 | 퇴사 상태 임직원에게 배정된 자산을 "미회수 자산"으로 표시한다 | Must |
| 15-4 | 해당 목록에서 바로 회수 처리 화면으로 이동할 수 있다 | Should |

> 이메일·메신저 알림은 v3(FR-26). v1은 화면 표출로 대체한다.

---

## 4. 화면 목록 (Screen Inventory)

| ID | 화면 | 주요 기능 | 우선순위 |
|---|---|---|---|
| SC-01 | 로그인 | FR-01 | Must |
| SC-02 | 대시보드 | FR-11, FR-15 | Must |
| SC-03 | 자산 목록 | FR-03, FR-05, FR-07 | Must |
| SC-04 | 자산 등록 | FR-02 | Must |
| SC-05 | 자산 상세 | FR-03, FR-10 | Must |
| SC-06 | 자산 수정 | FR-04 | Must |
| SC-07 | 배정 처리 (모달) | FR-08 | Must |
| SC-08 | 회수 처리 (모달) | FR-09 | Must |
| SC-09 | 상태 변경 (모달) | FR-12 | Must |
| SC-10 | 엑셀 가져오기 (마법사 3단계) | FR-06 | Must |
| SC-11 | 통합 이력 조회 | FR-10 | Should |
| SC-12 | 임직원 목록/상세 | FR-14 | Must |
| SC-13 | 공통코드 관리 | FR-13 | Must |
| SC-14 | 관리자 계정 관리 | FR-01 | Should |
| SC-15 | 비밀번호 변경 | FR-01 | Must |

### 4.1 네비게이션 구조

```
[대시보드]  [자산관리 ▾]  [임직원]  [이력]  [설정 ▾]        [김IT ▾]
                │                                  │
                ├ 자산 목록                        ├ 공통코드
                ├ 자산 등록                        └ 계정 관리
                └ 엑셀 가져오기
```

### 4.2 엑셀 가져오기 마법사 흐름

```
1단계: 파일 업로드          2단계: 컬럼 매핑 · 미리보기     3단계: 검증 결과 · 확정
┌──────────────────┐       ┌──────────────────────┐       ┌──────────────────────┐
│ 템플릿 다운로드   │       │ 엑셀컬럼 → 시스템필드 │       │ 총 1,204행           │
│                  │  →    │ 자산번호  → 자산번호   │  →    │ ✔ 정상   1,187       │
│ [파일 선택]      │       │ 자산구분  → 자산구분   │       │ ✖ 오류      17       │
│ 중복 처리: ○건너뜀│       │ ...                   │       │ [오류 리포트 받기]   │
│            ●갱신 │       │ [미리보기 20행]       │       │ [취소] [등록 실행]   │
└──────────────────┘       └──────────────────────┘       └──────────────────────┘
```

---

## 5. 주요 사용 시나리오

### 시나리오 1 — 기존 대장 이관 (최초 1회)

1. 관리자가 [설정 > 공통코드]에서 부서·사업장 코드를 사내 체계에 맞게 정리한다.
2. [임직원] 화면에서 인사팀 임직원 명단 엑셀을 가져온다.
3. [자산관리 > 엑셀 가져오기]에서 표준 템플릿을 다운로드하고, 기존 대장을 템플릿 형식에 맞춰 정리한다.
4. 파일을 업로드하고 컬럼을 매핑한다.
5. 검증 결과에서 오류 17건을 확인하고 오류 리포트를 내려받는다.
6. 엑셀에서 오류를 수정한 뒤 재업로드한다.
7. 오류 0건 확인 후 등록을 실행한다.
8. 대시보드에서 총 보유 대수가 기존 대장과 일치하는지 확인한다.

### 시나리오 2 — 신규 입사자 배정

1. 대시보드에서 `대기` 카드를 클릭해 미배정 자산 목록으로 이동한다.
2. 지급할 노트북을 찾아 상세로 들어간다.
3. [배정] 버튼 클릭 → 사번 `20260801` 입력 → 이름·부서·직급이 자동 입력된다.
4. 사업장·위치를 확인하고 지급일(오늘)을 그대로 둔 채 사유에 "신규 입사자 배정"을 입력한다.
5. 저장하면 상태가 `사용중`으로 바뀌고 `ASSIGN` 이력이 남는다.

### 시나리오 3 — 부서 이동

1. 통합 검색창에 직원 이름을 입력해 해당 직원의 자산을 찾는다.
2. 자산 상세 → [수정]에서 소속부서와 사업장·위치를 변경한다.
3. 변경 사유에 "생산기술팀 → 품질보증팀 부서 이동"을 입력하고 저장한다.
4. [이력] 탭에서 `MOVE` 이력으로 이전 부서 → 신규 부서가 기록된 것을 확인한다.

### 시나리오 4 — 퇴사자 장비 회수 및 재배정

1. [임직원]에서 퇴사자를 찾아 재직상태를 `퇴사`로 변경한다.
2. 시스템이 "배정된 자산 2건이 있습니다"를 경고하고 목록을 표시한다.
3. 목록에서 두 자산을 모두 선택하고 [일괄 회수]를 실행한다. 회수 사유=퇴사, 회수 후 상태=대기.
4. 두 자산이 `대기` 상태가 되어 재배정 가능한 상태가 된다.

### 시나리오 5 — 월간 경영진 보고

1. [자산 목록]에서 사업장=`전체`, 상태=`사용중` 필터를 적용한다.
2. [엑셀 내보내기] → `전체 항목` 선택 → 파일을 받는다.
3. 대시보드의 사업장별·부서별 집계를 캡처해 보고서에 첨부한다.

### 시나리오 6 — 노후 장비 폐기

1. 대시보드 [노후 자산] 목록에서 내용연수 초과 자산을 확인한다.
2. 해당 자산들을 회수 처리하여 `대기` 상태로 만든다.
3. [일괄 상태 변경]으로 `폐기예정`으로 전환한다. 사유="2026년 상반기 노후 장비 교체".
4. 실물 파기 완료 후 각 자산을 `폐기`로 전환하고 폐기일·폐기방법(HDD 물리파기)을 입력한다.
5. 대시보드 전체 보유 대수에서 해당 건수가 제외된 것을 확인한다.

---

## 6. 데이터 마이그레이션 계획

| 단계 | 작업 | 담당 | 비고 |
|---|---|---|---|
| M-1 | 기존 대장 현황 조사 (컬럼·건수·품질) | IT 관리자 | 열린 이슈 OI-04 |
| M-2 | 컬럼 매핑 정의서 작성 | IT 관리자 + 개발 | 기존 컬럼 → 시스템 필드 |
| M-3 | 코드값 정리 (부서·사업장 표준화) | IT 관리자 | 표기 흔들림 정리 ("생산1팀"/"생산 1팀") |
| M-4 | 임직원 명단 확보 및 가져오기 | 인사팀 협조 | OI-03 |
| M-5 | 시험 가져오기 (테스트 환경) | 개발 | 오류율 확인 |
| M-6 | 데이터 정제 | IT 관리자 | 오류 리포트 기반 |
| M-7 | 운영 가져오기 | 개발 | |
| M-8 | 건수·표본 검증 | IT 관리자 | 총 대수 일치, 표본 20건 필드 대조 |
| M-9 | 2주 병행 운영 | IT 관리자 | 엑셀·시스템 동시 갱신 |
| M-10 | 엑셀 대장 갱신 중단 | IT 관리자 | v1 완료 |

---

## 7. v1 요구사항 우선순위 요약

| 우선순위 | 항목 |
|---|---|
| **Must (필수)** | FR-01 ~ FR-14의 Must 항목 전체. 이 중 하나라도 빠지면 엑셀 대체 불가 |
| **Should (권장)** | 일괄 처리, 통합 이력 화면, 필터 칩, 검색 조건 URL 반영, 가져오기 되돌리기 |
| **Could (여유 시)** | 컬럼 선택 저장, 검색 조건 저장, 일괄 배정, 이력 포함 내보내기 |

---

## 8. v1 열린 이슈

Master §13의 이슈 중 v1 착수 전 반드시 확정해야 하는 항목.

| ID | 이슈 | 확정 필요 시점 |
|---|---|---|
| OI-01 | 자산번호 채번 규칙 (수기 vs 자동) | 설계 착수 전 |
| OI-02 | 부서 체계 (사업부/팀 2단계로 충분한가) | 설계 착수 전 |
| OI-03 | 임직원 명단 확보 가능 여부 및 갱신 주기 | 개발 중 |
| OI-04 | 기존 엑셀 대장 컬럼·건수·품질 | 설계 착수 전 |
| OI-05 | 공용 PC 배정 표현 방식 | 설계 착수 전 |
| OI-06 | 리스·임대 장비 존재 여부 | 설계 착수 전 |
| OI-07 | 폐쇄망 서버 사양 및 배포 절차 | 개발 착수 전 |
| OI-08 | 사번·성명 저장에 대한 개인정보 방침 | 설계 착수 전 |

---

## 9. 변경 이력

| 일자 | 버전 | 변경 내용 |
|---|---|---|
| 2026-08-26 | v0.1 | 최초 작성 |
