# PC 자산관리 시스템 v1 — 실행 및 운영 안내

`PRD-v1.md`(MVP)를 구현한 웹 애플리케이션이다. 설계 근거와 요구사항 대응은
[`docs/구현노트.md`](./docs/구현노트.md)를 참고한다.

---

## 1. 빠른 시작

### 필요 환경

| 항목 | 요구 |
|---|---|
| Python | 3.10 이상 (개발·검증은 3.12) |
| 브라우저 | Chrome / Edge 최신 2개 버전 (NFR-02) |
| 그 외 | 없음. Node.js·Docker·별도 DB 서버 불필요 |

### 설치

```bash
pip install -r requirements.txt
```

### 실행

```bash
python run.py
```

`http://127.0.0.1:8000` 으로 접속한다. 사내에 공개하려면:

```bash
python run.py --host 0.0.0.0 --port 8000
```

Windows에서는 `시작.bat`을 더블클릭해도 된다. 실행하면 접속 주소가 안내된다.

```
  PC 자산관리 시스템 v1 을 시작합니다.

    이 PC에서 접속    http://127.0.0.1:8000
    사내망에서 접속   http://192.168.0.5:8000

  종료하려면 이 창에서 Ctrl+C 를 누르세요.
```

> **`시작.bat` 수정 시 주의**: 이 파일은 **ASCII 문자만, CRLF 줄바꿈**으로 유지해야 한다.
> 한글을 넣거나 UTF-8(LF)로 저장하면 cmd가 명령을 잘못 해석해
> `'d'은(는) 내부 또는 외부 명령... 아닙니다` 같은 오류가 난다.
> 한글 안내는 파이썬(`run.py`)이 출력하므로 배치 파일에는 넣지 않는다.

### 최초 로그인

| 항목 | 값 |
|---|---|
| 아이디 | `admin` |
| 초기 비밀번호 | `admin1234` |

최초 로그인 시 **비밀번호 변경 화면으로 강제 이동**한다. 변경하면 세션이 종료되므로 새 비밀번호로 다시 로그인한다.

초기 계정 정보는 환경변수로 바꿀 수 있다 (DB가 비어 있을 때만 적용).

```bash
set PCAMS_ADMIN_ID=itadmin
set PCAMS_ADMIN_PW=Chang3Me!2026
python run.py
```

---

## 2. 데모 데이터

화면 확인·교육용 샘플(임직원 약 1,000명 / 자산 1,400대)을 만든다. **운영 DB를 덮어쓰므로 도입 전에만 사용한다.**

```bash
python run.py --reset-demo      # DB를 비우고 샘플 생성 후 기동
python tools/make_demo.py --reset
```

**현재 `data/assets.db`에는 데모 데이터가 들어 있다.** 실제 운영을 시작할 때는 이 파일을 지우고
기동하면 빈 DB(공통코드 + `admin` 계정만)로 새로 만들어진다.

```bash
del data\assets.db data\assets.db-wal data\assets.db-shm
python run.py
```

---

## 3. 자체 점검

PRD v1의 주요 요구사항을 API 시나리오로 확인한다. 운영 DB는 건드리지 않고 `data/selftest.db`를 임시로 만들어 쓴다.

```bash
pip install httpx2
python tools/selftest.py
```

111개 항목이 모두 `PASS`로 나와야 한다. 배포 전과 코드 수정 후에 실행한다.

---

## 4. 백업 (NFR-10)

```bash
python tools/backup.py                    # data/backup/ 에 생성, 30일 초과분 자동 삭제
python tools/backup.py --dir D:\백업 --keep-days 90
```

SQLite 온라인 백업 API를 사용하므로 서비스를 멈추지 않아도 안전하다.
**Windows 작업 스케줄러에 매일 1회 등록**해 두는 것을 권장한다.

- 프로그램: `python`
- 인수: `C:\LEEDATA\자산관리웹사이트\tools\backup.py`
- 시작 위치: `C:\LEEDATA\자산관리웹사이트`

### 복구 절차

1. 서비스를 정지한다 (`Ctrl+C`).
2. `data\assets.db`, `data\assets.db-wal`, `data\assets.db-shm`를 다른 이름으로 옮긴다.
3. 백업 파일을 `data\assets.db`로 복사한다.
4. 서비스를 다시 기동하고 대시보드 총 보유 대수를 확인한다.

---

## 5. 서버 배포

사내 서버 1대에 파이썬만 있으면 된다. 프런트엔드도 빌드 도구 없이 정적 파일을 그대로 서빙한다.

### 1) 설치와 기동

```bash
pip install -r requirements.txt
python run.py --host 0.0.0.0 --port 8000
```

### 2) 인터넷이 닫힌 서버에 넣어야 한다면

서버가 외부로 나갈 수 없는 환경이라면 오프라인 번들로 반입한다.

```bash
pip download -r requirements.txt -d wheels          # 인터넷 되는 PC에서
pip install --no-index --find-links wheels -r requirements.txt   # 서버에서
```

화면과 자산 CRUD는 외부 연결 없이도 그대로 동작한다. 외부 연동 기능만 비활성 상태가 된다. (§9 참조)

### 3) 상시 기동

Windows 서비스로 등록하려면 [NSSM](https://nssm.cc) 같은 도구를 쓰거나, 작업 스케줄러의
"시스템 시작 시" 트리거로 `시작.bat`을 실행한다. 리눅스라면 systemd 유닛을 쓴다.

```ini
# /etc/systemd/system/pcams.service
[Unit]
Description=PC 자산관리 시스템
After=network.target

[Service]
WorkingDirectory=/opt/pcams
ExecStart=/usr/bin/python3 run.py --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

> **주의**: HTTPS가 필요하면 사내 리버스 프록시(IIS / nginx) 뒤에 두고, 그때
> `app/routers/session_api.py`의 쿠키 설정에 `secure=True`를 추가한다.

---

## 6. 도입 순서 (PRD-v1 §5 시나리오 1 / §6 마이그레이션)

1. **공통코드 정리** — [설정 > 공통코드]에서 부서·사업장·제조사를 사내 체계에 맞춘다.
   기본 부서 코드는 예시이므로 실제 조직도로 교체한다.
2. **임직원 명단 가져오기** — [임직원 > 엑셀 가져오기 > 임직원 명단].
   먼저 표준 템플릿을 내려받아 인사팀 명단을 정리한다.
3. **시험 가져오기** — 기존 대장 일부(50~100행)로 오류율을 확인하고 코드 표기를 정리한다.
4. **운영 가져오기** — 전량을 올리고 `All-or-Nothing` 모드로 등록한다.
   오류가 있으면 오류 리포트를 내려받아 엑셀에서 고친 뒤 다시 올린다.
5. **검증** — 대시보드 총 보유 대수가 기존 대장과 일치하는지, 표본 20건의 필드가 맞는지 확인한다.
6. **2주 병행 운영** 후 엑셀 대장 갱신을 중단한다.

> 잘못 올렸다면 [엑셀 가져오기] 화면 아래 **최근 가져오기 배치**에서 **되돌리기**를 쓸 수 있다.
> (등록 이후 변경 이력이 생긴 자산은 안전을 위해 유지된다.)

---

## 7. 폴더 구조

```
자산관리웹사이트/
├─ app/                     백엔드 (FastAPI)
│  ├─ main.py               진입점 · 정적 서빙 · 예외 처리
│  ├─ core.py               상태 전환 규칙 · 값 검증 · 시간
│  ├─ auth.py               로그인 · 세션 · 비밀번호 (FR-01)
│  ├─ db.py / schema.sql    SQLite 연결 · 스키마
│  ├─ seed.py               공통코드 · 최초 관리자 시딩
│  ├─ routers/              API 엔드포인트
│  └─ services/             도메인 로직 (자산 · 검색 · 엑셀 · 가져오기)
├─ web/                     프런트엔드 (빌드 불필요)
│  ├─ index.html
│  ├─ css/app.css
│  └─ js/                   api · util · ui · prefs(개인설정) · app(라우터) · pages/
├─ tools/
│  ├─ selftest.py           자체 점검
│  ├─ make_demo.py          데모 데이터
│  └─ backup.py             DB 백업
├─ data/                    ← DB와 업로드 임시파일. 백업 대상
├─ run.py / 시작.bat
└─ requirements.txt
```

---

## 8. 자주 겪는 문제

| 증상 | 확인 |
|---|---|
| 화면이 비어 있다 | 브라우저 콘솔(F12)에 오류가 있는지 확인. 새로고침(Ctrl+F5)으로 캐시를 비운다 |
| 프로그램을 갱신했는데 화면이 그대로다 | 브라우저가 이전 화면 파일을 캐시한 것이다. **Ctrl+F5**로 강제 새로고침한다 |
| 로그인이 안 된다 | 5회 실패 시 10분 잠금(NFR-08). 다른 관리자 계정으로 [설정 > 계정 관리]에서 잠금 해제 |
| 관리자 계정을 모두 잃었다 | `data/assets.db`를 백업해 두고 `admin_user` 테이블을 비운 뒤 재기동하면 `admin` 계정이 다시 만들어진다 |
| 엑셀 업로드가 실패한다 | 5,000행 / 10MB 제한. `.xls`는 `xlrd` 설치 필요. 안 되면 `.xlsx`로 저장 후 재시도 |
| CSV 한글이 깨진다 | UTF-8 또는 CP949(엑셀 기본)로 저장. 둘 다 자동 인식한다 |
| 포트가 이미 사용 중 | 이미 켜둔 창에서 Ctrl+C로 종료하거나 `python run.py --port 8080` |
| `시작.bat` 실행 시 `'d'은(는) ... 아닙니다` 같은 오류 | 배치 파일이 UTF-8/LF로 저장되어 생기는 문제다. **`시작.bat`은 반드시 ASCII 문자만, CRLF 줄바꿈으로 저장**한다. 한글 안내는 `run.py`가 출력하므로 배치 파일에 한글을 넣지 않는다 |
